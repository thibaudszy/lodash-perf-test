import { describe, it } from 'node:test';
import assert from 'assert';
import subtract from 'lodash-es-with-subpaths/subtract';

describe('subtract', () => {
    it('should work as expected', () => {
        assert.strictEqual(typeof subtract, function);
    });
});